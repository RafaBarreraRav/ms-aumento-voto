name: CI

on:
  push:
    branches:
      - feature/pipeline-ayudantia

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      with:
        repository: https://github.com/DevOps-Fundamentos-v7/microservicio-java
